name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  avatar:
    name: Avatar web
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: web/avatar
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '24'
      - run: npm ci --ignore-scripts
      - run: npm run format:check
      - run: npm test
      - run: npm run build
      - run: npm --prefix ../../experiments/voice/realtime/web ci --ignore-scripts
      - run: node ../../experiments/voice/realtime/web/build.mjs
      - run: node --test ../../experiments/voice/realtime/web/*.test.mjs
      - run: npx --no-install prettier --check "../../experiments/voice/realtime/web/*.{js,mjs,html,css,json}" "!../../experiments/voice/realtime/web/bundle.js"
  check:
    name: ${{ matrix.os }} / Python ${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python: ['3.12', '3.13']
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: astral-sh/setup-uv@d0d8abe699bfb85fec6de9f7adb5ae17292296ff # v6
        with:
          version: '0.12.5'
          python-version: ${{ matrix.python }}
          enable-cache: true
      - run: uv sync --locked
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run pytest -q
      - run: uv sync --locked --extra agent --extra avatar --extra voice
      - run: uv run --extra agent --extra avatar --extra voice pytest -q experiments/voice/realtime
      - run: uv run --extra agent --extra avatar --extra voice pytest -q tests/test_mcp_transport.py tests/test_mcp_views.py tests/test_mcp_command_revision.py tests/test_ardy_contacts.py tests/test_ardy_origin.py tests/test_walk_tolerance.py tests/test_arm_reach.py tests/test_object_actions.py tests/test_avatar_reach.py tests/test_avatar_live.py tests/test_avatar_viewer.py tests/test_prepared_avatar.py tests/test_prepared_kinematic.py tests/test_continuous_motion.py tests/test_motion_history.py tests/test_appearance_checkpoint.py tests/test_memory.py tests/test_voice.py tests/test_initiative.py
      - run: uv build
      - run: uv run promethee demo --pause-after 4
      - run: uv run promethee demo --resume
      - run: uv run promethee journal
